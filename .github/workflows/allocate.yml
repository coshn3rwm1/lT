name: blank

on:
  push:

jobs:
  freedom:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        spit: ["probability", "countryside", "flock", "blank", "hire"]
        class: ["specify", "alliance", "disguise", "messenger"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar oA
